{
  "name": "novel-ai-os",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Executable Context/Canon vertical slice for Novel-AI-OS V5",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "demo": "node src/demo.ts",
    "validate": "node scripts/validate-memory.mjs && node --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
